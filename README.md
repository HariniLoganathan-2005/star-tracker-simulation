## Setup Instructions

1. Install Git LFS:

   ```
   git lfs install
   ```

2. Clone the repository:

   ```
   git clone https://github.com/HariniLoganathan-2005/star-tracker-simulation.git
   ```

3. Pull LFS files:

   ```
   git lfs pull
   ```

Now the project is ready to run.
